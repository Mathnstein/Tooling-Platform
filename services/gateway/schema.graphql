# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

input CancelJobInput {
  """The unique identifier of the job to be canceled"""
  id: ID!
}

input CreateJobInput {
  """The user who submitted the job"""
  submittedById: String!

  """The unique identifier of the tool to be executed"""
  toolId: String!

  """The input provided to the tool for processing"""
  toolInput: JSONObject!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.This scalar is serialized to a string in ISO 8601 format and parsed from a string in ISO 8601 format.
"""
scalar DateTimeISO

"""
The `JSONObject` scalar type represents JSON objects as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSONObject

"""
Represents a job submitted to the system for processing. Contains details about the job's parameters, status, and metadata.
"""
type Job {
  """The unique identifier of the job"""
  id: ID!

  """Indicates whether the job has been canceled"""
  isCanceled: Boolean

  """The current execution state of the job"""
  status: JobStatus!

  """The user who submitted the job"""
  submittedBy: User!

  """The time when the job was submitted"""
  timeSubmitted: DateTimeISO!

  """The unique identifier of the tool associated with the job"""
  toolId: String!

  """The input provided to the tool for processing"""
  toolInput: JSONObject!
}

"""The current execution state of a job"""
enum JobStatus {
  CANCELED
  COMPLETED
  FAILED
  PENDING
  PROCESSING
}

type Mutation {
  """Cancels a job by adding its ID to the canceled job store."""
  cancelJob(input: CancelJobInput!): Boolean!

  """Creates a new job and publishes it to the RabbitMQ queue."""
  createJob(input: CreateJobInput!): Job!

  """Reenables a job by removing its ID from the canceled job store."""
  reenableJob(input: ReenableJobInput!): Boolean!
}

type Query {
  """Fetches a list of canceled jobs from the store."""
  canceledJobs: [Job!]!

  """
  Fetches a list of jobs from the queue. Each job includes an `isCanceled` field that indicates whether the job has been canceled.
  """
  jobs: [Job!]!

  """Gets a list of tools available to the user."""
  tools: [Tool!]!
}

input ReenableJobInput {
  """The unique identifier of the job to be reenabled"""
  id: ID!
}

"""The type of user you are"""
enum RoleType {
  ADMIN
  USER
}

type Tool {
  """Indicates whether the user has access to the tool"""
  hasAccess: Boolean!

  """The unique identifier of the tool"""
  id: ID!

  """The name of the tool"""
  name: String!

  """The slug of the tool"""
  slug: String!
}

type User {
  email: String!
  id: ID!
  jobHistory: [Job]
  name: String!
  role: RoleType!
}